Bonjour, Madame <%= params[:order].first_name %> <%= params[:order].last_name %> 

Votre commande a bien ete pris en compte.
Vous recevrez une confirmation par email dans les prochaines 24 heures.

Nom: <%= params[:order].first_name %> <%= params[:order].last_name %> 
Telephone: <%= params[:order].phone %>
Email: <%= params[:order].email %>

Commande:
<% params[:order].order_items.each do |order_item| %>
  - <%= order_item.product.title %> (x<%= order_item.quantity %>)
<% end %>

-----
JustBeauty